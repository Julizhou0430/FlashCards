<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vike.flash.cards"
    android:versionCode="2"
    android:versionName="1.1" >

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name="vike.flash.cards.flash_cards"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="vike.flash.cards.Course"
            android:label="@string/course" >
        </activity>
        <activity
            android:name="vike.flash.cards.words"
            android:label="@string/Words" >
        </activity>
        <activity
            android:name="vike.flash.cards.welcome"
            android:label="@string/welcome" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="vike.flash.cards.setting"
            android:label="@string/setting" >
        </activity>
        <activity
            android:name="vike.flash.cards.test_words"
            android:label="@string/test" >
        </activity>
        <activity android:name="Preferences" >
        </activity>
    </application>

    <uses-sdk android:minSdkVersion="4" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RESTART_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

</manifest>